import { NextFunction, Request, Response } from "express";

export default class ConsentimientosService {
    async GenerarConsentimiento(req: Request, res: Response, next: NextFunction) {
        try {
            
        } catch (e) {
            
        }
    }
}